{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with dev settings",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYDEVD_WARN_SLOW_RESOLVE_TIMEOUT": "20.0"
      }
    }
  ]
}
